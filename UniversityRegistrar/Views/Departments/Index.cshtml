@{
  Layout = "_Layout";
}

@using UniversityRegistrar.Models;

<h1>Departments</h1>

@if (@Model.Count == 0)
{
  <h3>No courses have been added yet!</h3>
} 

<ul>
  @foreach(Course course in Model)
  {
    <li>@Html.ActionLink($"{course.DepartmentName}", "Details", new { id = department.DepartmentId })</li>
  }
</ul>

<p>@Html.ActionLink("Add new department", "Create")</p>
<p>@Html.ActionLink("Home", "Index", "Home")</p>