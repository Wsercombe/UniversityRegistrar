@{
  Layout = "_Layout";
}

@model UniversityRegistrar.Models.Student

<h2>Are you sure you want to terminate this student? Give it a second chance?</h2>

@Html.DisplayNameFor(model => model.StudentName): @Html.DisplayFor(model => model.StudentName)
@using (Html.BeginForm())
{
  <input type="submit" value="Terminate Student" class = "btn-btn-danger"/>
}
@Html.ActionLink("Back to List", "Index")